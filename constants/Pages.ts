import { Page } from '@types';

const Pages: Record<string, Page> = {
  HOME: 'Home',
  SCANNER: 'Scanner',
  EVENT_DETAILS: 'EventDetails',
  LOGIN: 'Login',
  SIGNUP: 'Signup'
};

export default Pages;
