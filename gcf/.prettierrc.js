module.exports = {
    trailingComma: 'es5', // Trailing commas where valid in ES5 (objects, arrays, etc.)
    tabWidth: 4, // Number of spaces per indentation level.
    semi: true, // Print semicolons.
    singleQuote: true, // Use single quotes instead of double quotes.
    printWidth: 100, // Approximate desired length (not a hard-cap)
  };
  